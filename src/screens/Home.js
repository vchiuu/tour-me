import React from 'react';
import { Text, TouchableOpacity } from 'react-native';

const Home = () => (
  <React.Fragment>
    <TouchableOpacity>
      <Text>Open the modal</Text>
    </TouchableOpacity>
  </React.Fragment>
);

export default Home;
